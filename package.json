{
  "name": "deep-dive-todo-assignment",
  "scripts": {
    "server": "live-server ./src --port=3001 --no-browser --watch",
    "database": "json-server --watch db.json",
    "dev":"run-p server database"
  },  
  "devDependencies": {
    "json-server": "^0.17.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
 